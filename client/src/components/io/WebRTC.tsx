const WebRTC = () => {

    // TODO: implement
    //useSetUpRTC()
        //check listener list
        //load specific rtc connection code based on user's status (check with server)

        // useEffect(() => {
//     //open socket connection with server
//     //server-side: register user as broadcaster with socket
//     //set up socket callback listeners for joiners
//     //handle RTC connection when listeners come in; createOffer
//     //enforce limit
// }, [])

    return null
}

export default WebRTC